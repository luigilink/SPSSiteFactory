# Azure Functions managed dependencies.
# These modules are restored from the PowerShell Gallery on the Function host.
# PnP.PowerShell 3.x requires the PowerShell 7.4 Functions runtime.
@{
    'PnP.PowerShell' = '3.*'
}
